<fieldset>
  {{~#if ctx.show}}
  <legend>{{ctx.legend}}</legend>
  {{#if ctx.showAction}}
  {{formGroup ctx.actionField name="consume.action" localize=true value=ctx.action}}
  {{/if}}
  {{#if ctx.showUses}}
  {{formGroup ctx.usesField name="consume.uses" localize=true value=ctx.uses}}
  {{/if}}
  {{/if~}}
</fieldset>
