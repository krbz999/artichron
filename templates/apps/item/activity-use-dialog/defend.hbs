<fieldset>
  {{~#if ctx.show}}
  <legend>{{ctx.legend}}</legend>
  <p class="defend">
    <span class="formula">{{ctx.formula}}</span>
  </p>

  {{formGroup ctx.field name="defend.increase" localize=true}}
  {{/if~}}
</fieldset>
