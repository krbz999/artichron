<section class="standard-form">
  {{#each ctx.chains}}

  {{#each this}}
  <div data-advancement-uuid="{{advancement.uuid}}">

    <h3 class="section-header">
      <span>{{advancement.name}}</span>
      {{#if isChoice}}
      <button type="button" class="icon fa-solid fa-fw {{ifThen capped 'fa-check' 'fa-cog'}}" data-action="changeOptionalAdvancement"></button>
      {{/if}}
    </h3>

    {{#each choices}}
    {{#if (or (not ../isChoice) (lookup ../selected item.id))}}
    <div class="form-group" data-item-uuid="{{item.uuid}}">
      <label>{{{link.outerHTML}}}</label>
    </div>
    {{/if}}
    {{/each}}
  </div>
  {{/each}}

  {{#unless @last}}
  <hr>
  {{/unless}}

  {{/each}}
</section>
