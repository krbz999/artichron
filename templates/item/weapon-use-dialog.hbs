<fieldset>
  {{#if ammo.show}}
  {{formGroup ammo.field dataset=ammo.dataset localize=true sort=true name="ammo"}}
  {{/if}}

  {{#if stamina.show}}
  {{formGroup stamina.field dataset=stamina.dataset localize=true name="stamina"}}
  {{/if}}

  {{formGroup booster localize=true name="booster"}}
  {{formGroup uses localize=true name="uses" disabled=true}}
</fieldset>
