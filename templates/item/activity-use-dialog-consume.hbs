<fieldset>
  {{~#if consume.show}}
  <legend>{{consume.legend}}</legend>
  {{#if consume.showAction}}
  {{formGroup consume.actionField name="consume.action" localize=true value=consume.action}}
  {{/if}}
  {{#if consume.showUses}}
  {{formGroup consume.usesField name="consume.uses" localize=true value=consume.uses}}
  {{/if}}
  {{/if~}}
</fieldset>
