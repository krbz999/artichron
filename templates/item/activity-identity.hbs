<div class="tab identity {{tabs.identity.cssClass}}" data-group="primary" data-tab="identity">
  <fieldset>
    <legend>{{localize "ARTICHRON.SheetTab.Identity"}}</legend>
    {{formGroup name.field value=name.value}}
    {{formGroup img.field value=img.value}}
    {{formGroup description.field value=description.value enriched=description.enriched height=200 toggled=true}}
  </fieldset>
</div>
