<fieldset>

  {{#if isDamage}}
  {{formGroup damage.field value=damage.value localize=true name=damage.name}}
  {{/if}}
  {{#if isBuff}}
  {{formGroup buff.field value=buff.value localize=true name=buff.name}}
  {{/if}}
  {{formGroup shape.field value=shape.value localize=true name=shape.name}}
  {{formGroup boosters localize=true name="boosters"}}

</fieldset>
