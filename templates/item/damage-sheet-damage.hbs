<fieldset>
  <legend>{{localize "ARTICHRON.SheetLabels.Damage"}}</legend>
  {{#each damageFields}}
  {{formGroup field value=value disabled=disabled type=type classes=classes}}
  {{/each}}
</fieldset>
