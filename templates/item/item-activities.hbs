<div class="{{tabs.activities.tabCssClass}}" data-group="primary" data-tab="activities">
  <fieldset class="activities">
    <legend>
      {{localize "ARTICHRON.SheetTab.Activities"}}
      {{#if isEditMode}}
      <button type="button" class="control" data-action="createActivity">
        <i class="fa-solid fa-plus"></i>
      </button>
      {{/if}}
    </legend>
    {{#each activities}}
    <div class="form-group activity" data-activity-id="{{id}}">
      <button type="button" data-action="renderActivity" {{disabled disabled}}>
        <span class="name">{{name}}</span>
        <span class="subtitle">{{subtitle}}</span>
      </button>
    </div>
    {{/each}}
  </fieldset>
</div>
