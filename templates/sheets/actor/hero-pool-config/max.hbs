<fieldset>
  <legend>{{localize "ARTICHRON.PoolConfig.MaxLabel"}}</legend>
  {{formGroup max.field name="max.config" classes="stacked" value=max.value}}
  {{#each max.selects}}{{formGroup field name=name blank=false value=value localize=true}}{{/each}}
</fieldset>
