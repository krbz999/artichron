{{!-- Sheet Tab Navigation --}}
<nav class="sheet-tabs tabs" data-group="primary">
  {{#each tabs}}
  <a data-tab="{{id}}" class="{{cssClass}}" data-action="tab" data-group="primary">
    <i class="{{icon}}"></i>
  </a>
  {{/each}}
</nav>
