{{!-- Sheet Tab Navigation --}}
<nav class="sheet-tabs tabs">
  {{#each tabs}}
  <a class="item {{cssClass}}" data-action="tab" data-tab="{{id}}" data-group="primary">{{localize label}}</a>
  {{/each}}
</nav>
