<div class="{{tabs.attributes.tabCssClass}}" data-group="primary" data-tab="attributes">

  <div class="left-pane">
    <fieldset class="pools">
      {{#if isEditMode}}
      <a class="edit" data-action="toggleConfig" data-tooltip="ARTICHRON.SheetActions.EditPools" data-trait="pools">
        <i class="fa-solid fa-cog"></i>
      </a>
      {{/if}}
      {{#each pools}}
      <a class="pool die d{{faces}} {{key}}" data-action="rollPool" data-pool="{{key}}" data-tooltip="{{value}} / {{max}} (d{{faces}})">
        <span class="label">{{value}}</span>
      </a>
      {{/each}}
    </fieldset>

    <fieldset class="health">
      <input type="text" name="system.health.value" class="delta" value="{{health.value}}" placeholder="0">
      <span class="sep">/</span>
      <span class="max">{{health.max}}</span>

      <div class="health-bar" style="--health-bar-pct: {{health.pct}}%"></div>
      <a class="control" data-action="recoverHealth" data-tooltip="ARTICHRON.SheetActions.RecoverHealth">
        <i class="fa-solid fa-heart"></i>
      </a>
    </fieldset>
  </div>

  <div class="center-pane scrollable">

    <fieldset class="favorites">
      <legend>{{localize "ARTICHRON.SheetLabels.Favorites"}}</legend>
      <div class="inventory-list compact">
        {{#each document.favorites}}{{inventoryItem this}}{{/each}}
      </div>
    </fieldset>

    <fieldset class="defenses">
      <legend>{{localize "ARTICHRON.SheetLabels.Defenses"}}</legend>
      {{#each defenses}}
      <div class="defense {{#if active}} active {{/if}}" data-tooltip="{{label}}" style="color: #{{color}}">
        <span class="total">{{value}}</span>
        <i class="{{icon}}"></i>
      </div>
      {{/each}}
    </fieldset>

    <fieldset class="skills">
      <legend>{{localize "ARTICHRON.SheetLabels.Skills"}}</legend>
      {{#each skills}}
      <a class="skill label-top" data-action="rollSkill" data-skill-id="{{skillId}}">
        <label>{{label}}</label>
        <img src="{{img}}" alt="{{label}}">
        <span class="value">{{value}}d6</span>
      </a>
      {{/each}}
    </fieldset>

  </div>

</div>
