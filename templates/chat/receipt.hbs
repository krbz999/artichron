<section class="artichron">

  <h3>{{localize "ARTICHRON.CHAT.receipt"}}</h3>
  <p>{{localize "ARTICHRON.CHAT.receiptSummary" name=name total=total shop=shop}}</p>
  {{#each customers}}

  <h3 class="section-header">
    <span>{{actor.link}}</span>
  </h3>
  {{#each items}}
  <p>{{link}}</p>
  {{/each}}

  {{/each}}

</section>
